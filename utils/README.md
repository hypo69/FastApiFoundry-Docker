# 🛠️ Утилиты (utils)

Эта директория содержит вспомогательные скрипты и утилиты для упрощения разработки и администрирования проекта.

---
**📚 Документация:** [Главное README](../README.md) | [Настройка SSL](../docs/ssl-setup.md)
---

## 📄 Содержимое

| Файл | Описание |
| :--- | :--- |
| **`ssl-generator.ps1`** | **(Windows)** PowerShell-скрипт для автоматической генерации самоподписанных SSL-сертификатов, необходимых для запуска сервера в режиме HTTPS на локальной машине. |

## 🚀 Использование `ssl-generator.ps1`

Этот скрипт предназначен для пользователей **Windows**. Он создает два файла (`key.pem` и `cert.pem`) и помещает их в директорию `~/.ssl/`, которую сервер использует по умолчанию.

### Запуск
Откройте PowerShell и выполните:
```powershell
# Перейти в директорию utils
cd utils

# Запустить генератор
.\ssl-generator.ps1
```

### Принудительная перегенерация
Если вам нужно пересоздать существующие сертификаты, используйте флаг `-Force`:
```powershell
.\ssl-generator.ps1 -Force
```

### Настройка для Linux/macOS
Пользователи Linux и macOS могут сгенерировать аналогичные сертификаты с помощью `openssl`:
```bash
mkdir -p ~/.ssl
openssl req -x509 -newkey rsa:2048 \
  -keyout ~/.ssl/key.pem \
  -out ~/.ssl/cert.pem \
  -days 365 -nodes \
  -subj "/CN=localhost"
```

Подробную информацию о настройке HTTPS вы найдете в **[руководстве по настройке SSL](../docs/ssl-setup.md)**.

---
**[⬆️ Назад к корневому README](../README.md)**