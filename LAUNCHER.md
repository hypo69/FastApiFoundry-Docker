# 🚀 FastAPI Foundry Launcher

**Интуитивный интерфейс для запуска FastAPI Foundry**

## 🎯 Быстрый запуск

```powershell
# Интерактивное меню
.\launcher.ps1

# Или прямой запуск
.\launcher.ps1 -Mode quick
```

## 📋 Доступные режимы

### 1. 🎯 Быстрый запуск (рекомендуется)
```powershell
.\launcher.ps1 -Mode quick
```
- Автоматическая установка зависимостей
- Запуск Foundry AI
- Запуск FastAPI сервера
- Загрузка переменных окружения

### 2. 🐍 Только FastAPI сервер
```powershell
.\launcher.ps1 -Mode api
```
- Запуск без Foundry
- Только веб-интерфейс
- Быстрый старт

### 3. 🔧 Режим разработки
```powershell
.\launcher.ps1 -Mode dev
```
- Подробный вывод
- Отладочная информация
- Для разработчиков

### 4. 🐳 Docker запуск
```powershell
.\launcher.ps1 -Mode docker
```
- Сборка Docker образа
- Запуск в контейнере
- Изолированная среда

### 5. ⚙️ Настройка окружения
```powershell
.\launcher.ps1 -Mode setup
```
- Настройка .env переменных
- Интерактивная конфигурация
- Генерация ключей

### 6. 🔍 Диагностика
```powershell
.\launcher.ps1 -Mode diag
```
- Проверка Python
- Проверка .env
- Проверка Foundry
- Проверка портов

## 🔄 Миграция со старых скриптов

| Старый файл | Новый способ |
|-------------|--------------|
| `start.ps1` | `.\launcher.ps1 -Mode quick` |
| `start_simple.ps1` | `.\launcher.ps1 -Mode dev` |
| `setup-env.ps1` | `.\launcher.ps1 -Mode setup` |
| `diagnose.py` | `.\launcher.ps1 -Mode diag` |

## 💡 Примеры использования

```powershell
# Первый запуск - настройка и запуск
.\launcher.ps1 -Mode setup
.\launcher.ps1 -Mode quick

# Разработка - только API
.\launcher.ps1 -Mode api

# Проблемы - диагностика
.\launcher.ps1 -Mode diag

# Продакшн - Docker
.\launcher.ps1 -Mode docker
```

---

**Теперь все типы запуска в одном месте! 🎉**