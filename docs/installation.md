# 📦 Установка

---
**📚 Навигация:** [🏠 Главная](README.md) | [📦 Установка](installation.md) | [🚀 Запуск](running.md) | [🎯 Лончеры](launchers.md) | [📚 Использование](usage.md) | [⚙️ Настройка](configuration.md) | [📊 Примеры](examples.md) | [🛠️ Рецепты](howto.md) | [🔌 MCP](mcp_integration.md) | [🌍 Туннели](tunnel_guide.md) | [🐳 Docker](docker.md) | [🛠️ Разработка](development.md) | [🚀 Развертывание](deployment.md) | [🔧 cURL](curl_commands.md) | [📋 Проект](project_info.md)

---

## Системные требования

### Минимальные требования
- **Python**: 3.8+ (рекомендуется 3.11+)
- **RAM**: 4GB (минимум), 8GB (рекомендуется)
- **Диск**: 2GB свободного места (10GB для RAG)
- **ОС**: Windows 10+, Linux (Ubuntu 20.04+), macOS 10.15+
- **Интернет**: Для скачивания зависимостей

### Опциональные компоненты
- **Foundry CLI**: Для работы с локальными AI моделями
- **Git**: Для клонирования репозитория (автоустановка)
- **Docker**: Для контейнеризации (автоустановка)
- **CUDA**: Для GPU ускорения RAG системы

### Порты
- **8000**: FastAPI Foundry API (по умолчанию)
- **50477**: Foundry сервер (если используется)

## ⚡ Автоматическая установка (рекомендуется)

### Способ 1: Через установочные скрипты

```bash
# Клонировать репозиторий
git clone https://github.com/hypo69/FastApiFoundry.git
cd FastApiFoundry

# Windows - запустить установщик
install.bat
# или
.\install.ps1

# Linux/macOS - Python установщик
python install.py
```

**Что происходит автоматически:**
1. ✅ Проверяется Python 3.8+
2. ✅ Создаётся виртуальное окружение (venv)
3. ✅ Обновляется pip
4. ✅ Устанавливаются все зависимости из requirements.txt
5. ✅ Проверяется Foundry CLI (опционально)
6. ✅ Создаются необходимые директории (logs, rag_index)
7. ✅ Создаётся .env файл из .env.example
8. ✅ Тестируется установка (FastAPI, Uvicorn)
9. ✅ Показываются следующие шаги

### Способ 2: Через лончеры (автоустановка при первом запуске)

```bash
# Windows - лончеры автоматически запускают install.ps1 если venv не существует
.\start-dev.ps1
# или
start.bat

# Linux/macOS - основной скрипт не делает автоустановку
python install.py  # сначала установка
python run.py --dev --ssl --auto-port  # потом запуск
```

## 🔧 Ручная установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/hypo69/FastApiFoundry.git
cd FastApiFoundry
```

### 2. Установка Foundry CLI

```bash
# Windows (через winget)
winget install Microsoft.Foundry

# Или скачать с официального сайта
# https://foundry.microsoft.com/

# Проверка установки
foundry --version
```

### 3. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/macOS
python -m venv venv
source venv/bin/activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка конфигурации

```bash
# Копировать пример конфигурации
cp .env.example .env

# Отредактировать настройки (опционально)
# notepad .env  # Windows
# nano .env     # Linux/macOS
```

### 6. Проверка установки

```bash
# Проверка Python зависимостей
python -c "import fastapi; print('FastAPI installed')"
python -c "import uvicorn; print('Uvicorn installed')"

# Проверка Foundry
foundry --version
foundry service status

# Тестовый запуск
python run.py --help

# Тестирование API клиентов
python example_client.py
python example_model_client.py
python example_mcp_client.py
python example_rag_client.py
```

## 🎯 Рекомендации

### Для новичков:
```bash
# Используйте автоматическую установку
install.bat  # Windows (самый простой)
python install.py  # Linux/macOS
```

### Для опытных пользователей:
```bash
# Ручная установка для полного контроля
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/macOS
pip install -r requirements.txt
```

### Для разработчиков:
```bash
# Установка с параметрами
.\install.ps1 -SkipFoundry  # пропустить Foundry
.\install.ps1 -Force  # переустановка
```

## 🔧 Устранение проблем

### Проблемы с Python

**Проблема**: `python: command not found`
```bash
# Windows
# Установите Python с python.org
# Отметьте "Add Python to PATH"

# Linux
sudo apt update && sudo apt install python3 python3-pip python3-venv

# macOS
brew install python3
```

**Проблема**: `ModuleNotFoundError: No module named 'venv'`
```bash
# Ubuntu/Debian
sudo apt install python3-venv

# CentOS/RHEL
sudo yum install python3-venv
```

### Проблемы с зависимостями

**Проблема**: `pip install` не работает
```bash
# Обновить pip
python -m pip install --upgrade pip

# Использовать другой индекс
pip install -r requirements.txt -i https://pypi.org/simple/

# Установка без кэша
pip install --no-cache-dir -r requirements.txt
```

**Проблема**: `Microsoft Visual C++ 14.0 is required`
```bash
# Windows - установить Visual Studio Build Tools
# Скачать с https://visualstudio.microsoft.com/visual-cpp-build-tools/

# Или использовать conda
conda install -c conda-forge fastapi uvicorn
```

### Проблемы с портами

**Проблема**: `Port 8000 is already in use`
```bash
# Найти процесс
netstat -ano | findstr :8000  # Windows
lsof -i :8000                 # Linux/macOS

# Использовать другой порт
python run.py --port 8001

# Автопоиск свободного порта
python run.py --auto-port
```

### Проблемы с Foundry

**Проблема**: `Foundry server not accessible`
```bash
# Проверить статус Foundry
foundry service status

# Запустить Foundry
foundry service start

# Проверить подключение
curl http://localhost:50477/v1/models
```

### Проблемы с RAG

**Проблема**: `RAG index not found`
```bash
# Создать RAG индекс
python rag_indexer.py

# Пересоздать индекс
python rag_indexer.py --rebuild

# Проверить состояние
ls -la rag_index/
```

### Проблемы с Docker

**Проблема**: `Docker daemon not running`
```bash
# Windows - запустить Docker Desktop

# Linux
sudo systemctl start docker
sudo systemctl enable docker

# Проверить статус
docker --version
docker ps
```

### Проблемы с правами

**Проблема**: `Permission denied`
```bash
# Linux/macOS - добавить права на выполнение
chmod +x install.sh
chmod +x docker-manager.sh

# Windows PowerShell - разрешить выполнение скриптов
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Общие рекомендации

1. **Проверьте логи**:
   ```bash
   tail -f logs/fastapi-foundry.log
   tail -f logs/fastapi-foundry-errors.log
   ```

2. **Перезапустите систему**:
   ```bash
   # Полная переустановка
   rm -rf venv/
   python install.py
   ```

3. **Проверьте сетевые настройки**:
   ```bash
   # Отключить файрволл/антивирус
   # Проверить VPN/прокси
   ```

4. **Обратитесь за помощью**:
   - GitHub Issues: https://github.com/hypo69/FastApiFoundry/issues
   - Приложите логи и описание проблемы

---

## 📚 Что дальше?

### ✅ Установка завершена! Следующие шаги:

1. **[🚀 Запуск](running.md)** - Основные способы запуска FastAPI Foundry
2. **[🎯 Лончеры](launchers.md)** - Детальное руководство по всем лончерам
3. **[⚙️ Настройка](configuration.md)** - Конфигурация через .env файл
4. **[📖 Использование](usage.md)** - Веб-интерфейс и REST API

### 🔗 Полезные ссылки:
- **[Примеры](examples.md)** - Готовые примеры кода
- **[Туннели](tunnel_guide.md)** - Публичный доступ через ngrok/Cloudflare
- **[Docker](docker.md)** - Контейнеризация и развертывание
- **[Разработка](development.md)** - Для разработчиков
- **[Развертывание](deployment.md)** - Production развертывание

### 📝 Примеры клиентов:
- **example_client.py** - Основной API клиент
- **example_model_client.py** - Управление моделями
- **example_mcp_client.py** - MCP клиент и интеграция
- **example_rag_client.py** - RAG система и поиск

---

## 📚 Навигация по разделу "Начало работы"

| Документ | Описание |
|----------|----------|
| [📦 Установка](installation.md) | Системные требования и установка |
| [🚀 Запуск](running.md) | Основные способы запуска |
| [🎯 Лончеры](launchers.md) | Детальное руководство по лончерам |
| [📚 Использование](usage.md) | Веб-интерфейс и REST API |
| [⚙️ Настройка](configuration.md) | Конфигурация через .env |

## 🔗 Другие разделы

| Раздел | Документы |
|--------|-----------||
| **🛠️ Практика** | [📊 Примеры](examples.md) • [🛠️ Рецепты](howto.md) |
| **🌐 Интеграция** | [🔌 MCP](mcp_integration.md) • [🌍 Туннели](tunnel_guide.md) |
| **🚀 Развертывание** | [🐳 Docker](docker.md) • [🚀 Deployment](deployment.md) |
| **👨‍💻 Разработка** | [🛠️ Development](development.md) • [🔧 cURL](curl_commands.md) • [📋 Проект](project_info.md) |

---

**📚 Быстрые ссылки:** [⬅️ Назад к оглавлению](README.md) | [📚 Все документы](README.md#-документация)

**FastAPI Foundry** - часть экосистемы AiStros  
© 2025 AiStros Team