# 🎮 Examples - Запуск примеров из веб-панели

## ✅ Что добавлено

### Новая вкладка "Examples" в веб-панели
- ✅ Запуск примеров одним кликом
- ✅ Просмотр вывода в реальном времени
- ✅ Цветовая подсветка результатов
- ✅ Статус выполнения
- ✅ Список доступных примеров

## 🎯 Доступные примеры

### 1. API Client Demo (`examples/example_client.py`)
**Что делает:**
- Проверка здоровья системы
- Список доступных моделей
- RAG поиск
- Конфигурация системы
- Управление моделями
- Статус туннеля

**Когда использовать:**
- Проверка работоспособности API
- Тестирование всех endpoints
- Демонстрация возможностей

### 2. RAG Search Demo (`examples/example_rag_client.py`)
**Что делает:**
- Поиск в документации проекта
- Демонстрация RAG системы
- Контекстный поиск

**Когда использовать:**
- Тестирование RAG
- Поиск информации в документах
- Проверка индекса

### 3. MCP Client Demo (`examples/example_mcp_client.py`)
**Что делает:**
- Подключение к MCP серверу
- Инициализация протокола
- Список инструментов
- Вызов инструментов
- Получение ресурсов

**Когда использовать:**
- Тестирование MCP интеграции
- Проверка Claude Desktop подключения
- Отладка MCP сервера

### 4. Model Client Demo (`examples/example_model_client.py`)
**Что делает:**
- Работа с моделями
- Генерация текста
- Управление моделями

**Когда использовать:**
- Тестирование моделей
- Проверка генерации
- Отладка Foundry

## 🌐 Использование веб-интерфейса

### Запуск примера
1. Откройте http://localhost:8000
2. Перейдите на вкладку **"Examples"**
3. Нажмите на кнопку нужного примера
4. Дождитесь выполнения
5. Просмотрите результаты

### Интерфейс
- **Кнопки примеров** - Запуск конкретного примера
- **Example Output** - Вывод выполнения в реальном времени
- **Example Status** - Текущий статус (Running/Completed/Failed)
- **Available Examples** - Список всех примеров с файлами
- **Clear Output** - Очистка вывода

### Цветовая кодировка
- 🔴 **Красный** - Ошибки (ERROR, ❌)
- 🟡 **Желтый** - Предупреждения (WARNING, ⚠️)
- 🟢 **Зеленый** - Успех (SUCCESS, ✅)
- 🔵 **Синий** - Информация (INFO, 🔍, 📊)
- ⚪ **Серый** - Обычный текст

## 📊 API для запуска примеров

### Запуск примера

```bash
curl -X POST http://localhost:8000/api/v1/examples/run \
  -H "Content-Type: application/json" \
  -d '{"example_type": "client"}'
```

**Ответ:**
```json
{
  "success": true,
  "example_type": "client",
  "output": "🚀 FastAPI Foundry Client Demo\n...",
  "execution_time": 2.34,
  "return_code": 0
}
```

### Список примеров

```bash
curl http://localhost:8000/api/v1/examples/list
```

**Ответ:**
```json
{
  "success": true,
  "examples": [
    {
      "type": "client",
      "name": "API Client Demo",
      "file": "example_client.py",
      "description": "Демонстрация всех API endpoints",
      "available": true,
      "size": 12345
    }
  ],
  "total_count": 4,
  "available_count": 4
}
```

## 🔧 Использование в Python

```python
from example_client import FastAPIFoundryClient

async with FastAPIFoundryClient() as client:
    # Запустить пример
    result = await client.run_example("client")
    print(result["output"])
    
    # Список примеров
    examples = await client.list_examples()
    print(f"Available: {examples['available_count']}")
```

## ⚙️ Технические детали

### Таймаут выполнения
- **60 секунд** - максимальное время выполнения
- При превышении пример автоматически останавливается

### Логирование
Все запуски примеров логируются:
```python
logger.info("Запуск примера", example_type="client")
logger.info("Пример выполнен", execution_time=2.34)
```

### Безопасность
- Примеры запускаются в отдельном процессе
- Ограничение по времени выполнения
- Изоляция вывода

## 🎓 Примеры использования

### Быстрая проверка системы
1. Запустите "API Client Demo"
2. Проверьте вывод на наличие ошибок
3. Убедитесь, что все endpoints работают

### Тестирование RAG
1. Запустите "RAG Search Demo"
2. Проверьте результаты поиска
3. Убедитесь, что индекс загружен

### Отладка MCP
1. Запустите "MCP Client Demo"
2. Проверьте подключение к серверу
3. Убедитесь, что инструменты доступны

## 📝 Добавление своих примеров

### 1. Создайте файл примера
```python
# examples/example_custom.py
async def main():
    print("🚀 Custom Example")
    # Ваш код здесь
    
if __name__ == "__main__":
    asyncio.run(main())
```

### 2. Добавьте в API
```python
# examples_endpoints.py
example_files = {
    "custom": "examples/example_custom.py"
}
```

### 3. Добавьте кнопку в веб-интерфейс
```html
<button onclick="runExample('custom')">
    Custom Example
</button>
```

## ✅ Чек-лист

- [ ] Все примеры запускаются без ошибок
- [ ] Вывод корректно отображается
- [ ] Цветовая кодировка работает
- [ ] Статус обновляется
- [ ] Таймаут срабатывает корректно

---

**Примеры готовы к использованию!** 🎉