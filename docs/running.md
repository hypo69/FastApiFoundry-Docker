# 🚀 Запуск

> **Предыдущий шаг**: [📦 Установка](installation.md) - Установка FastAPI Foundry

## Быстрый старт

```bash
# Активировать виртуальное окружение
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/macOS

# Запустить в режиме разработки (по умолчанию)
python run.py
```

## Режимы запуска

### Development режим
```bash
python run.py --dev
# или просто
python run.py
```
- Auto-reload включен
- Debug логирование
- 1 worker процесс
- Подробные ошибки

### Production режим
```bash
python run.py --prod
```
- Auto-reload отключен
- Info логирование  
- 4 worker процесса
- Оптимизированная производительность

### Кастомные параметры
```bash
python run.py --host 0.0.0.0 --port 8080 --workers 2 --log-level info
```

## Дополнительные опции

### SSL/HTTPS
```bash
# Использовать сертификаты по умолчанию
python run.py --ssl

# Указать свои сертификаты
python run.py --ssl-keyfile server.key --ssl-certfile server.crt
```

### Автопоиск портов
```bash
# Автоматически найти свободные порты
python run.py --auto-port
```

### Запуск с MCP Console
```bash
# Запустить FastAPI + MCP Console
python run.py --mcp --browser
```

## Проверка запуска

### Основные endpoints
```bash
# Главная страница
curl http://localhost:8000/

# Проверка здоровья
curl http://localhost:8000/api/v1/health

# API документация
open http://localhost:8000/docs
```

### Логи
```bash
# Просмотр логов в реальном времени
tail -f logs/fastapi-foundry.log
```

## ⚡ Быстрый старт (всё в одной команде)

```bash
python run.py --dev --ssl --auto-port
```

**Что происходит:**
1. ✅ Запускается FastAPI Foundry на HTTPS
2. ✅ Автоматически запускается Foundry service
3. ✅ Автоматически находятся свободные порты
4. ✅ Обновляется конфигурация с правильным портом Foundry

### Варианты быстрого запуска

```bash
# Production (высокая производительность)
python run.py --prod --ssl --auto-port

# Development с отладкой
python run.py --dev --ssl --auto-port --log-level debug

# Без HTTPS (если проблемы с сертификатами)
python run.py --dev --auto-port
```

## 🚀 Различные способы запуска

### 1. run.py - Основной лончер (рекомендуется)

**Назначение**: Полнофункциональный запуск FastAPI Foundry сервера

**Возможности**:
- Автоматический запуск Foundry service
- Поддержка HTTPS с SSL сертификатами  
- Автопоиск свободных портов
- Режимы dev/prod
- Интеграция с MCP Console (если доступна)
- Автооткрытие браузера
- Полное логирование

```bash
# Простой запуск (dev режим по умолчанию)
python run.py

# Development с HTTPS и автопортами
python run.py --dev --ssl --auto-port

# Production режим
python run.py --prod --ssl

# Полный набор опций
python run.py --dev --ssl --mcp --auto-port --browser
```

### 2. PowerShell лончеры (Windows)

#### start-dev.ps1 - Режим разработки
```powershell
.\start-dev.ps1
# Эквивалент: python run.py --dev --ssl --mcp --auto-port --browser
```

#### start-prod.ps1 - Production режим
```powershell
.\start-prod.ps1
# Эквивалент: python run.py --prod --ssl --mcp --auto-port
```

#### start-https.ps1 - HTTPS с параметрами
```powershell
# Базовый HTTPS запуск
.\start-https.ps1

# С дополнительными опциями
.\start-https.ps1 -Mode prod -Mcp -AutoPort -Browser
```

#### start-simple.ps1 - Упрощенный лончер
```powershell
# Минимальная обертка над run.py
.\start-simple.ps1 --dev --ssl --mcp
```

### 3. Batch лончеры (Windows)

#### start.bat - Универсальный
```batch
start.bat --dev --ssl --mcp --auto-port
```

#### start-prod.bat - Production
```batch
start-prod.bat
# Эквивалент: python run.py --prod --ssl --mcp --auto-port
```

#### start-simple.bat - Упрощенный
```batch
start-simple.bat --dev --ssl
```

### 4. start_with_tunnel.py - Туннельный лончер

**Назначение**: Запуск с публичным доступом через туннели

**Возможности**:
- Поддержка ngrok, Cloudflare, LocalTunnel, Serveo
- Автоматический запуск FastAPI + туннель
- Кастомные поддомены
- Интеграция с режимами dev/prod
- Поддержка SSL и MCP

```bash
# С ngrok (по умолчанию)
python start_with_tunnel.py --ssl --mcp

# С Cloudflare туннелем
python start_with_tunnel.py --tunnel cloudflared --mode prod

# С кастомным поддоменом
python start_with_tunnel.py --tunnel ngrok --subdomain myapp --ssl

# Только туннель (FastAPI уже запущен)
python start_with_tunnel.py --tunnel-only --port 8443
```

### Когда использовать какой лончер

**Используйте run.py когда:**
- Разрабатываете локально
- Нужны все возможности (HTTPS, автопорты, MCP)
- Хотите полный контроль над настройками

**Используйте start_with_tunnel.py когда:**
- Нужен публичный доступ к API
- Тестируете с внешними сервисами
- Демонстрируете проект удаленно

## Остановка

```bash
# Ctrl+C в терминале
# или
pkill -f "python run.py"
```

---

## 📚 Что дальше?

### ✅ Сервер запущен! Следующие шаги:

1. **[🎯 Лончеры](launchers.md)** - Полное руководство по всем способам запуска
2. **[📖 Использование](usage.md)** - Веб-интерфейс и REST API
3. **[⚙️ Настройка](configuration.md)** - Конфигурация через .env файл
4. **[📊 Примеры](examples.md)** - Готовые примеры кода

### 🔗 Полезные ссылки:
- **[🌐 Туннели](tunnel_guide.md)** - Публичный доступ через ngrok/Cloudflare
- **[🔧 Разработка](development.md)** - Для разработчиков
- **[🚀 Развертывание](deployment.md)** - Production развертывание

### 🔍 Проверка работы:
```bash
# Проверка здоровья API
curl https://localhost:8443/api/v1/health

# Открыть веб-интерфейс
open https://localhost:8443/

# API документация
open https://localhost:8443/docs
```