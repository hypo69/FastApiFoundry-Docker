# 🌐 Руководство по туннелям для FastAPI Foundry

**Как сделать ваш локальный FastAPI Foundry доступным из интернета**

## 🎯 Зачем нужны туннели?

Туннели позволяют:
- 🌍 Получить доступ к локальному серверу из интернета
- 🔗 Поделиться API с коллегами
- 📱 Тестировать на мобильных устройствах
- 🤖 Интегрировать с внешними сервисами (webhooks)

## 🚀 Рекомендуемые сервисы

### 1. 🥇 ngrok (Лучший выбор)

**Плюсы**: Стабильный, быстрый, много функций  
**Минусы**: Ограничения в бесплатной версии

#### Установка:
```bash
# Windows (Chocolatey)
choco install ngrok

# Или скачать с https://ngrok.com/download
```

#### Использование:
```bash
# Базовый туннель
ngrok http 8000

# С аутентификацией (после регистрации)
ngrok config add-authtoken YOUR_TOKEN
ngrok http 8000

# С кастомным поддоменом (платная версия)
ngrok http 8000 --subdomain my-api
```

#### Результат:
```
Session Status                online
Account                       your@email.com (Plan: Free)
Version                       3.1.0
Region                        United States (us)
Latency                       45ms
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123.ngrok.io -> http://localhost:8000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

### 2. ☁️ Cloudflare Tunnel (Профессиональный)

**Плюсы**: Бесплатный, от Cloudflare, очень быстрый  
**Минусы**: Требует больше настройки

#### Установка:
```bash
# Windows
winget install Cloudflare.cloudflared

# Или скачать с GitHub
```

#### Быстрый старт:
```bash
# Временный туннель
cloudflared tunnel --url http://localhost:8000
```

#### Постоянный туннель:
```bash
# 1. Авторизация
cloudflared tunnel login

# 2. Создание туннеля
cloudflared tunnel create my-fastapi-tunnel

# 3. Настройка DNS (если есть домен)
cloudflared tunnel route dns my-fastapi-tunnel api.yourdomain.com

# 4. Запуск
cloudflared tunnel run my-fastapi-tunnel
```

### 3. ⚡ LocalTunnel (Простой)

**Плюсы**: Очень простой, кастомные поддомены  
**Минусы**: Менее стабильный

#### Установка:
```bash
npm install -g localtunnel
```

#### Использование:
```bash
# Базовый туннель
lt --port 8000

# С кастомным поддоменом
lt --port 8000 --subdomain my-fastapi-api
```

### 4. 🔧 Serveo (Без установки)

**Плюсы**: Не требует установки, работает через SSH  
**Минусы**: Ограниченный функционал

```bash
# Туннель через SSH
ssh -R 80:localhost:8000 serveo.net

# С кастомным поддоменом
ssh -R my-api:80:localhost:8000 serveo.net
```

## 🛠️ Автоматический запуск

### Использование скрипта:

```bash
# С ngrok (по умолчанию)
python start_with_tunnel.py

# С Cloudflare
python start_with_tunnel.py --tunnel cloudflared

# С LocalTunnel и кастомным поддоменом
python start_with_tunnel.py --tunnel localtunnel --subdomain my-api

# Только туннель (FastAPI уже запущен)
python start_with_tunnel.py --tunnel-only
```

### Batch файл для Windows:

Создайте `start_tunnel.bat`:
```batch
@echo off
echo Starting FastAPI Foundry with ngrok tunnel...

REM Запуск FastAPI в фоне
start /B python run.py --dev

REM Ждем запуска FastAPI
timeout /t 5 /nobreak > nul

REM Запуск ngrok
ngrok http 8000

pause
```

## 🔐 Безопасность при использовании туннелей

### 1. Обязательно используйте API ключи

В `.env`:
```env
API_KEY=your-super-secret-key-here
```

### 2. Ограничьте CORS

```env
CORS_ORIGINS=["https://your-allowed-domain.com"]
```

### 3. Мониторинг доступа

Следите за логами:
```bash
tail -f logs/fastapi-foundry.log
```

### 4. Временные туннели

Для тестирования используйте временные туннели, не постоянные.

## 📊 Сравнение сервисов

| Сервис | Бесплатно | Кастомный домен | Стабильность | Скорость | Простота |
|--------|-----------|-----------------|--------------|----------|----------|
| **ngrok** | ✅ (лимиты) | 💰 Платно | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Cloudflare** | ✅ | ✅ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **LocalTunnel** | ✅ | ✅ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Serveo** | ✅ | ✅ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

## 🎯 Рекомендации по выбору

### Для разработки и тестирования:
**ngrok** - самый надежный и функциональный

### Для демонстрации клиентам:
**Cloudflare Tunnel** - профессиональный вид

### Для быстрых тестов:
**LocalTunnel** или **Serveo** - быстро и просто

### Для продакшн (временно):
**Cloudflare Tunnel** с собственным доменом

## 🔧 Примеры использования

### 1. Тестирование мобильного приложения

```bash
# Запустить туннель
ngrok http 8000

# Использовать URL в мобильном приложении
# https://abc123.ngrok.io/api/v1/generate
```

### 2. Webhook для внешнего сервиса

```bash
# Запустить туннель
cloudflared tunnel --url http://localhost:8000

# Настроить webhook URL
# https://random.trycloudflare.com/api/v1/webhook
```

### 3. Демонстрация API коллегам

```bash
# Запустить с кастомным поддоменом
lt --port 8000 --subdomain my-demo-api

# Поделиться ссылкой
# https://my-demo-api.loca.lt/docs
```

## 🚨 Устранение проблем

### ngrok не запускается
```bash
# Проверить установку
ngrok version

# Переустановить
choco uninstall ngrok
choco install ngrok
```

### Cloudflared ошибки
```bash
# Обновить до последней версии
cloudflared update

# Проверить статус
cloudflared tunnel list
```

### LocalTunnel не работает
```bash
# Переустановить
npm uninstall -g localtunnel
npm install -g localtunnel

# Попробовать другой порт
lt --port 8001
```

### Общие проблемы

1. **Порт занят**: Проверьте что FastAPI запущен на правильном порту
2. **Firewall**: Отключите firewall или добавьте исключения
3. **Антивирус**: Добавьте туннель в исключения

## 📱 Мобильное тестирование

После создания туннеля можно тестировать API на мобильных устройствах:

```javascript
// В мобильном приложении
const API_BASE = 'https://abc123.ngrok.io';

fetch(`${API_BASE}/api/v1/generate`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer your-api-key'
  },
  body: JSON.stringify({
    prompt: 'Hello from mobile!'
  })
});
```

## 🎉 Готовые команды

### Быстрый старт с ngrok:
```bash
cd FastApiFoundry
python start_with_tunnel.py
```

### Быстрый старт с Cloudflare:
```bash
cd FastApiFoundry
python start_with_tunnel.py --tunnel cloudflared
```

### Только туннель (FastAPI уже запущен):
```bash
python start_with_tunnel.py --tunnel-only --port 8000
```

---

**Теперь ваш FastAPI Foundry доступен из любой точки мира! 🌍**