# 🚀 Лончеры FastAPI Foundry

> **Предыдущие шаги**: [📦 Установка](installation.md) → [🚀 Запуск](running.md)

Полное руководство по всем способам запуска FastAPI Foundry.

## 📋 Сравнение лончеров

| Лончер | Платформа | venv | SSL | MCP | Auto-port | Простота |
|--------|-----------|------|-----|-----|-----------|----------|
| `run.py` | Все | ❌ | ✅ | ✅ | ✅ | ⭐⭐⭐ |
| `start-dev.ps1` | Windows | ✅ | ✅ | ✅ | ✅ | ⭐⭐⭐⭐⭐ |
| `start-prod.ps1` | Windows | ✅ | ✅ | ✅ | ✅ | ⭐⭐⭐⭐⭐ |
| `start-simple.ps1` | Windows | ✅ | ⚙️ | ⚙️ | ⚙️ | ⭐⭐⭐⭐ |
| `start.bat` | Windows | ✅ | ⚙️ | ⚙️ | ⚙️ | ⭐⭐⭐ |
| `start_with_tunnel.py` | Все | ❌ | ✅ | ✅ | ✅ | ⭐⭐ |

**Легенда**: ✅ - Включено по умолчанию, ⚙️ - Настраивается, ❌ - Не поддерживается

## 🎯 Рекомендации по выбору

### Для разработки (Windows):
```powershell
.\start-dev.ps1  # Самый простой способ
```

### Для production (Windows):
```powershell
.\start-prod.ps1  # Оптимизированные настройки
```

### Для кроссплатформенности:
```bash
python run.py --dev --ssl --mcp --auto-port --browser
```

### Для публичного доступа:
```bash
python start_with_tunnel.py --ssl --mcp --auto-port
```

### Для минимального запуска:
```bash
python run.py  # Только FastAPI, без дополнительных сервисов
```

## 📊 Детальное описание

### 1. run.py - Основной лончер

**Возможности:**
- Автоматический запуск Foundry service
- Поддержка HTTPS с SSL сертификатами
- Автопоиск свободных портов
- Режимы dev/prod
- Интеграция с MCP Console
- Автооткрытие браузера
- Полное логирование

**Примеры:**
```bash
# Простой запуск
python run.py

# Development с полным набором функций
python run.py --dev --ssl --mcp --auto-port --browser

# Production режим
python run.py --prod --ssl --mcp --auto-port

# Кастомные настройки
python run.py --host 0.0.0.0 --port 9000 --workers 8
```

### 2. PowerShell лончеры (Windows)

#### start-dev.ps1
- Автоактивация venv
- Режим разработки с полным набором функций
- HTTPS + MCP + автопорты + браузер

#### start-prod.ps1
- Автоактивация venv
- Production режим
- HTTPS + MCP + автопорты

#### start-https.ps1
- Гибкие параметры SSL
- Поддержка кастомных сертификатов
- Дополнительные опции

#### start-simple.ps1
- Минимальная обертка над run.py
- Автоактивация venv
- Передача всех параметров

### 3. Batch лончеры (Windows)

#### start.bat
- Универсальный лончер
- Автопроверка и установка venv
- Использует PowerShell внутри

#### start-prod.bat
- Production режим
- Автоактивация venv
- Фиксированные настройки

#### start-simple.bat
- Упрощенная версия
- Минимальные проверки

### 4. start_with_tunnel.py - Туннельный лончер

**Возможности:**
- Поддержка ngrok, Cloudflare, LocalTunnel, Serveo
- Автоматический запуск FastAPI + туннель
- Кастомные поддомены
- Интеграция с режимами dev/prod
- Поддержка SSL и MCP

**Примеры:**
```bash
# С ngrok
python start_with_tunnel.py --ssl --mcp --auto-port

# С Cloudflare
python start_with_tunnel.py --tunnel cloudflared --mode prod

# С кастомным поддоменом
python start_with_tunnel.py --tunnel ngrok --subdomain myapp

# Только туннель (FastAPI уже запущен)
python start_with_tunnel.py --tunnel-only --port 8443
```

## 🔧 Параметры запуска

### Основные параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--dev` | Режим разработки | Да |
| `--prod` | Production режим | Нет |
| `--ssl` | Включить HTTPS | Нет |
| `--mcp` | Запустить MCP Console | Нет |
| `--auto-port` | Автопоиск портов | Нет |
| `--browser` | Открыть браузер | Нет |
| `--host` | Хост для привязки | 0.0.0.0 |
| `--port` | Порт FastAPI | 8443 |
| `--mcp-port` | Порт MCP Console | 8765 |

### SSL параметры

| Параметр | Описание |
|----------|----------|
| `--ssl` | Использовать сертификаты из ~/.ssh/ |
| `--ssl-keyfile` | Путь к приватному ключу |
| `--ssl-certfile` | Путь к сертификату |

### Режимы работы

| Режим | Reload | Workers | Log Level | Описание |
|-------|--------|---------|-----------|----------|
| `dev` | ✅ | 1 | debug | Разработка |
| `prod` | ❌ | 4 | info | Production |

## 🌐 Адреса по умолчанию

После запуска доступны следующие адреса:

- **FastAPI**: https://localhost:8443
- **MCP Console**: https://localhost:8765
- **API Docs**: https://localhost:8443/docs
- **Web UI**: https://localhost:8443/

При использовании `--auto-port` порты определяются автоматически.

## 🛠️ Устранение неполадок

### Проблемы с venv
```bash
# Если venv не активируется автоматически
.\venv\Scripts\Activate.ps1
python run.py
```

### Проблемы с SSL
```bash
# Генерация самоподписанных сертификатов
openssl req -x509 -newkey rsa:2048 -keyout ~/.ssh/server.key -out ~/.ssh/server.crt -days 365 -nodes
```

### Проблемы с портами
```bash
# Использовать автопоиск портов
python run.py --auto-port

# Или указать конкретные порты
python run.py --port 9000 --mcp-port 9001
```

### Проблемы с Foundry
```bash
# Проверить статус Foundry
foundry service status

# Перезапустить Foundry
foundry service restart
```

## 📝 Примеры использования

### Быстрый старт для разработки
```powershell
# Windows
.\start-dev.ps1

# Кроссплатформенно
python run.py --dev --ssl --mcp --auto-port --browser
```

### Production запуск
```powershell
# Windows
.\start-prod.ps1

# Кроссплатформенно
python run.py --prod --ssl --mcp --auto-port
```

### Публичный доступ через туннель
```bash
# С ngrok
python start_with_tunnel.py --ssl --mcp --auto-port

# С Cloudflare
python start_with_tunnel.py --tunnel cloudflared --ssl --mcp
```

### Минимальный запуск
```bash
# Только FastAPI без дополнительных сервисов
python run.py --port 8000

# С HTTPS но без MCP
python run.py --ssl --port 8443
```

---

---

## 📚 Что дальше?

### ✅ Лончеры настроены! Следующие шаги:

1. **[📖 Использование](usage.md)** - Веб-интерфейс и REST API
2. **[⚙️ Настройка](configuration.md)** - Конфигурация через .env файл
3. **[📊 Примеры](examples.md)** - Готовые примеры кода
4. **[🌐 Туннели](tunnel_guide.md)** - Публичный доступ

### 🔗 Для разработчиков:
- **[🔧 Разработка](development.md)** - Архитектура и добавление функций
- **[🚀 Развертывание](deployment.md)** - Docker, Nginx, systemd
- **[📊 Проект](project_info.md)** - Детальная информация

---

**FastAPI Foundry Launchers** - часть экосистемы AiStros  
© 2025 AiStros Team