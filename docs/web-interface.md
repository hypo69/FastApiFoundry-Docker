# 🌐 Веб-интерфейс FastAPI Foundry

## 📋 Обзор интерфейса

FastAPI Foundry предоставляет удобный веб-интерфейс для управления AI моделями и взаимодействия с ними.

**Доступ**: http://localhost:9696 (после запуска)

## 🏠 Главная страница

### Основные разделы
- **AI Chat** - Интерактивный чат с AI моделями
- **Control Panel** - Управление Foundry и моделями
- **API Documentation** - Swagger UI документация
- **Examples** - Готовые примеры использования

### Быстрые ссылки
```
http://localhost:9696/                    # Главная
http://localhost:9696/static/chat.html    # Чат
http://localhost:9696/static/control.html # Управление
http://localhost:9696/docs               # API документация
```

## 💬 AI Chat Interface

### Возможности чата
- **Выбор модели** из доступных в Foundry
- **Настройка параметров** (temperature, max_tokens)
- **История сообщений** в рамках сессии
- **Очистка истории** одним кликом
- **Копирование ответов** в буфер обмена

### Использование
1. Откройте http://localhost:9696/static/chat.html
2. Выберите модель из выпадающего списка
3. Настройте параметры генерации
4. Введите сообщение и нажмите "Send"

### Параметры генерации
```javascript
{
  "temperature": 0.7,    // Креативность (0.0-1.0)
  "max_tokens": 2048,    // Максимум токенов
  "top_p": 0.9,         // Nucleus sampling
  "top_k": 40           // Top-K sampling
}
```

## ⚙️ Control Panel

### Управление Foundry
- **Service Status** - Статус Foundry сервиса
- **Start/Stop** - Запуск и остановка Foundry
- **Port Information** - Информация о портах
- **Health Check** - Проверка работоспособности

### Управление моделями
- **Available Models** - Список доступных моделей
- **Load Model** - Загрузка модели в память
- **Unload Model** - Выгрузка модели
- **Model Status** - Статус загруженных моделей

### Мониторинг
- **System Resources** - Использование ресурсов
- **Request Statistics** - Статистика запросов
- **Error Logs** - Журнал ошибок

## 📊 API Documentation

### Swagger UI
Доступен по адресу: http://localhost:9696/docs

### Основные endpoints
```
GET  /api/v1/health           # Проверка здоровья
GET  /api/v1/models           # Список моделей
POST /api/v1/generate         # Генерация текста
POST /api/v1/chat             # Чат с AI
GET  /api/v1/foundry/status   # Статус Foundry
```

### Интерактивное тестирование
1. Откройте http://localhost:9696/docs
2. Выберите endpoint
3. Нажмите "Try it out"
4. Заполните параметры
5. Нажмите "Execute"

## 🔧 Настройки интерфейса

### config.json - веб-интерфейс
```json
{
  "web_interface": {
    "api_base": "http://localhost:9696/api/v1",
    "auto_refresh_interval": 30000,
    "logs_refresh_interval": 10000,
    "max_chat_history": 100
  }
}
```

### Параметры
- `api_base` - Базовый URL для API запросов
- `auto_refresh_interval` - Интервал обновления статуса (мс)
- `logs_refresh_interval` - Интервал обновления логов (мс)
- `max_chat_history` - Максимум сообщений в истории чата

## 📱 Адаптивный дизайн

Веб-интерфейс адаптирован для:
- **Desktop** - Полная функциональность
- **Tablet** - Упрощенный интерфейс
- **Mobile** - Базовые функции чата

## 🔐 Безопасность

### CORS настройки
```json
{
  "security": {
    "cors_origins": ["*"],
    "api_key": null
  }
}
```

### API ключи (опционально)
```json
{
  "security": {
    "api_key": "your-secret-key"
  }
}
```

## 🛠️ Кастомизация

### Статические файлы
```
static/
├── index.html      # Главная страница
├── chat.html       # Чат интерфейс
├── control.html    # Панель управления
├── app.js          # Основная логика
└── simple.html     # Упрощенный интерфейс
```

### Модификация интерфейса
1. Отредактируйте файлы в папке `static/`
2. Перезапустите сервер
3. Обновите страницу в браузере

## 📊 Примеры использования

### Простой чат
```javascript
// Отправка сообщения через JavaScript
fetch('/api/v1/chat', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    message: "Привет!",
    model: "qwen2.5-0.5b-instruct-generic-cpu:4"
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

### Генерация текста
```javascript
// Генерация текста
fetch('/api/v1/generate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    prompt: "Напиши стихотворение",
    max_tokens: 100,
    temperature: 0.8
  })
})
.then(response => response.json())
.then(data => console.log(data.text));
```

## 🔍 Отладка интерфейса

### Консоль браузера
1. Откройте Developer Tools (F12)
2. Перейдите на вкладку Console
3. Проверьте ошибки JavaScript

### Network мониторинг
1. Откройте вкладку Network
2. Выполните запрос
3. Проверьте статус ответа

---

**Следующий шаг**: [API Reference](api-reference.md)