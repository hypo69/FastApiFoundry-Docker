# 🌐 Tunnel Management API

---
**📚 Навигация:** [🏠 Главная](../README.md) | [📡 API](../api.md) | [🤖 Генерация](generation.md) | [🧠 Модели](models.md) | [🔍 RAG](rag.md) | [📊 Мониторинг](monitoring.md)

---

Этот API предназначен для управления публичными туннелями (например, ngrok, Cloudflare) для предоставления временного доступа к вашему локальному серверу из интернета.

## POST `/api/v1/tunnel/start`

Запускает туннель для указанного порта.

**Параметры запроса (query parameters):**
- `tunnel_type` (string, optional, default: `ngrok`): Тип туннеля. Возможные значения: `ngrok`, `cloudflared`, `localtunnel`, `serveo`.
- `port` (int, optional, default: `8000`): Локальный порт, на который будет перенаправляться трафик.
- `subdomain` (string, optional): Запрашиваемый поддомен (поддерживается не всеми сервисами).

**Пример запроса:**
```bash
curl -X POST "http://localhost:8000/api/v1/tunnel/start?tunnel_type=ngrok&port=8443"
```

**Ответ (`200 OK`):**
```json
{
  "success": true,
  "message": "Tunnel started successfully.",
  "data": {
    "tunnel_type": "ngrok",
    "public_url": "https://random-string.ngrok.io",
    "local_port": 8443,
    "status": "active"
  }
}
```

## POST `/api/v1/tunnel/stop`

Останавливает активный туннель.

**Параметры:** Нет

**Ответ (`200 OK`):**
```json
{
  "success": true,
  "message": "Tunnel stopped successfully.",
  "data": {
    "status": "inactive"
  }
}
```

## GET `/api/v1/tunnel/status`

Возвращает текущий статус туннеля.

**Параметры:** Нет

**Ответ (`200 OK`, если туннель активен):**
```json
{
  "success": true,
  "data": {
    "tunnel_type": "ngrok",
    "public_url": "https://random-string.ngrok.io",
    "local_port": 8443,
    "status": "active"
  }
}
```

**Ответ (`200 OK`, если туннель неактивен):**
```json
{
  "success": true,
  "data": {
    "status": "inactive"
  }
}
```

## 📖 Связанная документация

- **[🌐 Руководство по туннелям](../tunnel_guide.md)**: Подробное руководство по настройке и использованию различных туннельных сервисов.
- **[🚀 Скрипт `start_with_tunnel.py` ](../launchers.md#4-start_with_tunnelpy---туннельный-лончер)**: Утилита для одновременного запуска сервера и туннеля.

---
**[⬆️ Назад к документации по API](../api/README.md)**
